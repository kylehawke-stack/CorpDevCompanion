[build]
  command = "npm run build"
  publish = "dist"

[functions]
  # Max timeout for the analyze-company function (heavy data pipeline)
  external_node_modules = ["@anthropic-ai/sdk"]

[functions."analyze-company"]
  timeout = 60

[functions."generate-briefing"]
  timeout = 60

[functions."generate-company-ideas"]
  timeout = 60

[functions."generate-seed-ideas"]
  timeout = 60

[functions."generate-ideas"]
  timeout = 60

[functions."generate-narrative"]
  timeout = 60

[functions."inject-ideas"]
  timeout = 60

[functions."fetch-peer-data"]
  timeout = 60

[dev]
  command = "npm run dev"
  targetPort = 5173
  framework = "#custom"
  timeout = 120

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
